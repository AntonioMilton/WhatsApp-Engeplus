{
  "name": "whatsapp-suporte-ti",
  "version": "1.0.0",
  "description": "Bot de atendimento WhatsApp (Meta Cloud API) que triga com IA e guia o cliente ao chamado certo no Jira (portal SUP).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
